---
- name: Clone dotfiles repo
  git:
    repo: https://github.com/overtune/dotfiles.git
    dest: $HOME/.dotfiles
    bare: yes
  become_user: johrun

- name: Installs oh-my-zsh. 
  git:
    repo: https://github.com/robbyrussell/oh-my-zsh.git
    dest: $HOME/.oh-my-zsh
  become_user: johrun

- name: Checks out the config files to home dir. 
  command: /usr/bin/git --git-dir=$HOME/.dotfiles/ --work-tree=$HOME checkout 
  args:
    creates: $HOME/.vimrc
  become_user: johrun

- name: Install Vim plugins
  command: vim -E -s -c "source $HOME/.vimrc" -c PlugInstall -c qa
  args:
    creates: $HOME/.vim/plugged
  become_user: johrun

- name: Download wallpaper 
  command: wget -P $HOME/Pictures/ -O wallpaper.jpg https://wallpapersite.com/images/wallpapers/annapurna-massif-3840x2160-himalayas-nepal-mountains-sunrise-minimal-8112.jpg
  args:
    creates: $HOME/Pictures/wallpaper.jpg
  become_user: johrun

